<article class="ma-stat-card">
    <span class="ma-stat-card__accent" aria-hidden="true"></span>
    <p>{{ $label }}</p>
    <strong>{{ number_format($value) }}</strong>
    <small>{{ $hint }}</small>
</article>
